export class Game {
    gameId: string;
    name: string;
    imageUrl: string;

    constructor(gameId: string, name: string, imageUrl: string) {
        this.gameId = gameId;
        this.name = name;
        this.imageUrl= imageUrl;
    }
}

export class GameModel {

}